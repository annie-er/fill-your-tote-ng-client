export interface ContactMessage {
  fullName: string;
  pronouns?: string;
  email: string;
  company?: string;
  websiteOrProfile?: string;
  dueDate?: string;
  budget?: string;
  message: string;
  attachmentUrl?: string;
}